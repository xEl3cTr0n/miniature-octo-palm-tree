services:
  api:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ..:/app
    command: sh -c "pip install -e . && uvicorn claimlens.api.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
